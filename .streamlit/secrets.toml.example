# Copy to .streamlit/secrets.toml only in the target deployment environment.
# Keep secrets out of Git. Streamlit Cloud should store these values in the app settings UI.

RIP_APP_LANG_MODE = "international"
RIP_API_AUTH_MODE = "demo"
RIP_API_RATE_LIMIT_PER_MINUTE = "60"
